# brain-extension

## Install
```shell
python3.6 -m venv venv
source venv/bin/activate

pip install -r requirements.txt

mkdir data
echo '{}' > data/items.json
```

## Run
```shell
python server.py
```
